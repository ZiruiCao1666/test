# Notes on Clerk Expo SDK version

The original template referenced `@clerk/clerk-expo@^7.6.4`, which does not exist.
Use a current 2.x version instead, for example:

    npm i @clerk/clerk-expo@latest

This project has been updated to `^2.17.1` which is available on npm as of Nov 2025.